#include <stdio.h>
#include <string.h>
 
void main()
{
    char str[200];
    int y = 0, x;
 
   
    scanf("%[^\n]str", str);
    for (x = 0;str[x] != '\0';x++)
    {
        if (str[x] == ' ' && str[x+1] != ' ')
            y++;    
    }
    printf("No. Of Words: %d\n", y + 1);
}
